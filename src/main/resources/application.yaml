spring:
  application:
    name: "test"
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mesh_service_test2?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&createDatabaseIfNotExist=true
    username: xxxxx
    password: xxxxx
mybatis:
  type-aliases-package: com.example.demo.pojo
  mapper-locations: classpath:/mybatis/*.xml
logging:
  file:
    name: log/log.log
  level:
    root: info
    hugo: debug
